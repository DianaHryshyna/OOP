#pragma once
#include <string>
#include <vector>

class Person
{
public:
	Person(string name, int age);
	bool operator>(vector<Person> child1, vector<Pes)
	~Person();
private:
	string name;
	int age;
	vector<Person*> children;
};

